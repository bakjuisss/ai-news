# Vercel Environment Variables (Settings → Environment Variables)

GEMINI_API_KEY=your_gemini_api_key

# 이메일: Vercel에 SMTP 설정 불필요
# 사용자가 화면에서 받을 이메일 + 앱 비밀번호를 입력하면
# Nodemailer(오픈소스)로 해당 메일 SMTP 서버에 직접 연결해 전송합니다.
